[phases.setup]
nixPkgs = [
  "nodejs",
  "npm",
  "chromium",
  "nss",
  "freetype",
  "harfbuzz",
  "ca-certificates",
  "ttf-dejavu",
  "libxkbcommon"
]
[phases.setup]
aptPkgs = ["chromium", "chromium-driver"]

[variables]
PUPPETEER_EXECUTABLE_PATH = "/usr/bin/chromium"

[start]
cmd = "node index.js"

